name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  genesis:
    name: genesis chain (Linux x86-64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh genesis/test.sh
